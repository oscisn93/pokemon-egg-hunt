export enum GameEventType {
  READY,
  START,
  OVER,
  EGG_HATCHED,
}

export enum PokemonEggStatus {
  HIDDEN,
  VISIBLE,
  HATCHED,
  FOUND,
}

export enum PlayerEventType {
  READY,
  JOIN,
  IDLE,
  LEAVE,
  MOVEMENT,
  COLLISSION,
}
